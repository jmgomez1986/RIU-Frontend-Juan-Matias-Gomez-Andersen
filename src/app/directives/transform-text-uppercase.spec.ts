import { TransformTextUppercase } from './transform-text-uppercase';

describe('TransformTextUppercase', () => {
  it('should create an instance', () => {
    const directive = new TransformTextUppercase();
    expect(directive).toBeTruthy();
  });
});
